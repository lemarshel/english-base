/* English Base — offline news reader data.
   Generated output goes here; empty until the build pipeline exists, which
   makes the reader open on an empty state rather than fail. */
window.NEWS_DATA = { generated_at: null, channels: [] };
